
export const searchResultSelector = state => state.search.searchResult;

export const selectedStationSelector = state => state.selectedStation.station;

export const liveDetailSelector = state => state.selectedStation.liveDetails;
